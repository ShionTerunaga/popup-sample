import { style } from "@vanilla-extract/css"

const styles = {
    button: style({
        width: 150,
        height: 50,
        fontSize: 26,
        backgroundColor: "silver",
        color: "white",
        border: "none",
        borderRadius: 10,
        cursor: "pointer"
    })
}

export default styles
